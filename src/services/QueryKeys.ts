enum QueryKeys {
  WORKSPACES = 'workspaces',
  USER_PREFERENCES = 'user-preferences',
  PROJECTS = 'projects',
  PROJECT = 'project',
  ISSUE = 'issue',
  EDGE_SET = 'edget-set',
}

export default QueryKeys;
