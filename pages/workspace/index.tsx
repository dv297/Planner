import { TextField } from '@mui/material';
import Layout from '../../components/Layout';

const Workspace = () => {
  return (
    <Layout>
      <h1>Add Workspace</h1>
      <TextField />
    </Layout>
  );
};

export default Workspace;
